# Keep images as separate files instead of base64-inlining them into the JS
# bundle. After the tech icons were downscaled most fell under CRA's default
# 10KB threshold, which moved ~60KB of below-the-fold artwork onto the
# render-blocking bundle. As files they stay lazily fetched.
IMAGE_INLINE_SIZE_LIMIT=0
