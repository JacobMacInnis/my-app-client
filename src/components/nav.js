import React, { Component } from 'react';
import './nav.css';

class Nav extends Component {
  render() {
    return (
      <nav className="nav">
        <header className="App-header"> 
        </header>
      </nav>
    );
  }
}

export default Nav;